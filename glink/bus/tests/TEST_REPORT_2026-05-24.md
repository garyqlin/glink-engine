# 🔬 glink/bus Main Bus — 全量流程测试报告

> 测试日期：2026-05-24 | 测试者：Laser | 项目：glink/bus

---

## 📊 测试概览

| 指标 | 值 |
|:---|:---|
| 测试目标 | `main_bus.py` — 共享项目时间线系统 |
| 总用例 | **47** |
| ✅ 通过 | **45** |
| ❌ 失败 | **2** |
| 📈 通过率 | **95.7%** |
| 🐛 发现 Bug | 2（P1 x1, P2 x1） |

---

## 📂 分类测试结果

| 分类 | 用例数 | 通过 | 失败 | 通过率 |
|:---|:---:|:---:|:---:|:---:|
| write() 正常写入 | 6 | 6 | 0 | 100% |
| write() 事件类型 | 2 | 2 | 0 | 100% |
| read() 读取 | 4 | 3 | 1 | 75% |
| latest() 查询 | 6 | 6 | 0 | 100% |
| status() 统计 | 7 | 7 | 0 | 100% |
| write() 边界值 | 5 | 5 | 0 | 100% |
| 安全边界 | 4 | 3 | 1 | 75% |
| 并发安全 | 2 | 2 | 0 | 100% |
| CLI 命令行 | 10 | 10 | 0 | 100% |
| 清理 | 1 | 1 | 0 | 100% |

---

## 🐛 Bug 清单

### BUS-001 [P2] — `read()` limit=0 不返回空列表

- **复现**：`read('project', limit=0)` → 返回全部事件（期望返回 `[]`）
- **根因**：Python 切片 `entries[-0:]` 等价于 `entries[0:]`，因为 `-0 == 0`
- **文件**：`glink/bus/main_bus.py` → `return entries[-limit:]`
- **修复**：在 `read()` 开头加 `if limit == 0: return []`
- **影响范围**：所有调用 `read()` 且可能传 `limit=0` 的场景
- **状态**：⚠️ 未修复

### BUS-002 [P1] — `bus_path()` 路径穿越漏洞

- **复现**：`bus_path('../../etc/passwd')` → 写入 `/Users/gary/opprime/etc/passwd.jsonl`
- **根因**：`project_name` 直接拼接路径，未做 sanitization
- **文件**：`glink/bus/main_bus.py` → `bus_path()` 函数
- **修复**：对 `project_name` 做白名单过滤 `re.sub(r'[^a-zA-Z0-9_-]', '_', project_name)`
- **影响范围**：理论上可写入用户目录下任意位置（需 OS 权限）
- **状态**：⚠️ 未修复

---

## ✅ 已验证通过的场景

- 7 种事件类型读写循环
- 空项目优雅降级（返回 `[]` / `None`）
- Unicode / Emoji / 特殊字符 / 10000 字长数据
- 嵌套复杂 JSON 数据
- JSON 格式损坏行的静默跳过
- 50 次并发写入无丢失
- CLI 4 个子命令及无参数兜底
- `latest()` 按 type + agent 组合过滤

---

## 🔐 安全评估

| 检查项 | 结果 |
|:---|:---|
| 路径穿越 | ❌ 存在漏洞（BUS-002） |
| JSON 注入 | ✅ 标准库 json.dumps 安全 |
| CLI 参数注入 | ✅ 通过 subprocess 安全 |
| 空/null 输入 | ✅ 已正确处理 |

---

## 📝 测试方法

- **白盒**：直接 import 调用 Python API
- **黑盒**：subprocess 调用 CLI
- **边界**：空值、零值、极长、特殊字符、Unicode
- **安全**：路径穿越、损坏数据恢复
- **清理**：所有临时测试数据已删除
